export class GameMap {
    constructor(scene, camera) {
        this.scene = scene;
        this.camera = camera;
        this.objects = [];
    }
    static loadFromFile(filename) {
        // TODO: fill
        return null;
    }
    static loadMesh(filename) {
        // TODO: fill
        return null;
    }
}
