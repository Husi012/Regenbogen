class GameMap {
    constructor(scene, camera) {
        this.scene = scene;
        this.camera = camera;
        this.objects = [];
    }
    static loadFromFile(filename) {
        // TODO: fill
    }
    static loadMesh(filename) {
        // TODO: fill
    }
}
