

class Map {
  constructor(scene, camera) {
    this.scene = scene;
    this.camera = camera;
    this.objects = [];
  }
}

Map.loadFromFile = function(filename) {
  // TODO: fill
}

Map.loadMesh = function(filename) {
  // TODO: fill
}
